import { Component, ChangeDetectionStrategy, inject, signal } from '@angular/core';
import { CommonModule } from '@angular/common';
import { HttpClient } from '@angular/common/http';
import { PageHeaderComponent } from '../../shared/ui/page-header.component';
import { NotifyService, errMsg } from '../../shared/ui/notify.service';
import { environment } from '../../../environments/environment';
@Component({selector:'app-reports',standalone:true,changeDetection:ChangeDetectionStrategy.OnPush,imports:[CommonModule,PageHeaderComponent],template:`<div class="lf-page"><app-page-header title="التقارير التشغيلية والمالية" subtitle="مؤشرات الفواتير والمدفوعات والاشتراكات" icon="pi pi-chart-line"></app-page-header>@if(data();as d){<div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4"><div class="lf-card lf-stat p-5"><div class="text-sm text-slate-500">إجمالي الفواتير</div><b class="text-3xl">{{d.invoiceCount|number}}</b></div><div class="lf-card lf-stat p-5"><div class="text-sm text-slate-500">قيمة الفواتير</div><b class="text-3xl">{{d.invoicedAmount|number:'1.2-2'}}</b></div><div class="lf-card lf-stat p-5"><div class="text-sm text-slate-500">المبالغ المحصلة</div><b class="text-3xl text-green-600">{{d.collectedAmount|number:'1.2-2'}}</b></div><div class="lf-card lf-stat p-5"><div class="text-sm text-slate-500">طلبات دفع بانتظار المراجعة</div><b class="text-3xl text-amber-600">{{d.pendingPaymentRequests|number}}</b></div><div class="lf-card lf-stat p-5"><div class="text-sm text-slate-500">اشتراكات نشطة</div><b class="text-3xl text-primary-600">{{d.activeSubscriptions|number}}</b></div><div class="lf-card lf-stat p-5"><div class="text-sm text-slate-500">اشتراكات منتهية</div><b class="text-3xl text-red-600">{{d.expiredSubscriptions|number}}</b></div></div>}@else if(!loading()){<div class="lf-card p-8 text-center">لا تتوفر بيانات التقرير</div>}</div>`})
export class ReportsComponent{private http=inject(HttpClient);private notify=inject(NotifyService);data=signal<any|null>(null);loading=signal(true);ngOnInit(){this.http.get<any>(`${environment.apiUrl}/reports/overview`).subscribe({next:x=>{this.data.set(x);this.loading.set(false)},error:e=>{this.notify.error(errMsg(e));this.loading.set(false)}})}}
